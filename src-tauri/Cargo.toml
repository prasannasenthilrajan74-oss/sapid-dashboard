[package]
name = "skilltrack-analyzer"
version = "0.1.0"
description = "SkillTrack Analyzer - Cross-Platform HR Training Dashboard"
authors = []
edition = "2021"

[lib]
name = "skilltrack_analyzer_lib"
crate-type = ["rlib"]

[[bin]]
name = "skilltrack-analyzer"
path = "src/main.rs"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-opener = "2"
tauri-plugin-http = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
reqwest = { version = "0.12", features = ["json", "rustls-tls"], default-features = false }
tokio = { version = "1", features = ["full"] }
url = "2"

[profile.release]
panic = "abort"
codegen-units = 1
lto = true
opt-level = "s"
strip = true

[profile.dev]
debug = 0
incremental = false
split-debuginfo = "off"
opt-level = 0
